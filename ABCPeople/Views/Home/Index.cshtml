@model AbcPeople.ViewModels.HomepageViewModel

@{
    ViewData["Title"] = "Home Page";
}

<div class="container">
    <h4>Laatst aangepaste profielen</h4>
    <p>
        Via deze applicatie kan je een persoonlijk profiel aanmaken dat door iedereen binnen ABC-Groep kan opgezocht en bekeken worden. Het is aan de hand van dit profiel dat onze Sales medewerkers automatisch een CV kunnen genereren, daarom is het dus belangrijk om je profiel steeds up-to-date te houden.
        Je profiel bestaat uit je <a href="#">algemene info</a>, <a href="#">werkervaringen</a>, <a href="#">competenties</a> en <a href="#">overige gegevens</a>.
    </p>
    <hr />
    <div class="row">
        <div class="col-lg-7">
            <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
                <div class="carousel-inner">
                    <div class="carousel-item active">
                        <img class="img-fluid mx-auto d-block rounded" src="./images/Unknown-female.jpg" alt="First slide">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>iets1</h5>
                            <p>descr1</p>
                        </div>
                    </div>
                    <div class="carousel-item">
                        <img class="img-fluid mx-auto d-block rounded" src="./images/Unknown-male.jpg" alt="Second slide">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>iets2</h5>
                            <p>descr2</p>
                        </div>
                    </div>
                    <div class="carousel-item">
                        <img class="img-fluid mx-auto d-block rounded" src="./images/Unknown-female.jpg" alt="Third slide">
                        <br />
                        <div class="carousel-caption bg-dark" style="opacity: 0.4;">
                            <h5>iets3</h5>
                            <p>descr3</p>
                        </div>
                    </div>

                    @{
                        if (Model.ProfileAdjustments != null)
                        {
                            @foreach (var profileAdjustment in Model.ProfileAdjustments)
                            {
                                <div class="carousel-item">
                                    <img class="img-fluid mx-auto d-block rounded" src="./images/Unknown-female.jpg" alt="Third slide">
                                    <br />
                                    <div class="carousel-caption bg-dark" style="opacity: 0.4;">
                                        <h5></h5>
                                        <p>Aangepast op: @profileAdjustment.Timestamp</p>
                                    </div>
                                </div>
                            }
                        }
                    }

                </div>
                <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
        </div>
        <div class="col-lg-5">
            <div class="card" style="width: 21rem;">
                <div class="card-header">
                    <strong>Verjaardagen</strong>
                </div>
                <table class="table table-striped">
                    <tbody>
                        @foreach (var employee in @Model.Employees.Where(x => x.DateOfBirth.Month == DateTime.Now.Month))
                        {
                            <tr>
                                <td><a href="#">@employee.FirstName @employee.LastName</a> wordt @(DateTime.Now.Year - employee.DateOfBirth.Year)</td>
                                <td>
                                    @employee.DateOfBirth.Day @System.Globalization.CultureInfo.CurrentCulture.DateTimeFormat.GetMonthName(employee.DateOfBirth.Month)
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
